{
  "name": "@build-gym/04-runtime-chunk",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:bad": "webpack --mode production --config webpack.config.bad.js",
    "build:good": "webpack --mode production --config webpack.config.good.js",
    "build:rspack": "rspack build -c rspack.config.js",
    "build": "pnpm run build:bad",
    "bench": "node ../../scripts/benchmark.js"
  },
  "benchConfig": {
    "distDir": "dist",
    "baselineKey": "04-runtime-chunk"
  }
}
